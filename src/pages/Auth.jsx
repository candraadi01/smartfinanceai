import { Link, useNavigate } from 'react-router-dom';
import { WalletCards } from 'lucide-react';
export default function Auth({mode}){const nav=useNavigate();const isReg=mode==='register';function submit(e){e.preventDefault();nav('/onboarding')}return <div className="auth-page"><div className="auth-card"><div className="brand center"><div className="brand-mark"><WalletCards size={24}/></div><b>SmartFinance <span>AI</span></b></div><h1>{isReg?'Buat akun baru':'Selamat datang kembali'}</h1><p>{isReg?'Mulai kelola keuanganmu lebih cerdas.':'Masuk untuk melihat dashboard keuanganmu.'}</p><form onSubmit={submit}>{isReg&&<label>Nama<input required placeholder="Nama lengkap"/></label>}<label>Email<input required type="email" placeholder="nama@email.com"/></label><label>Password<input required type="password" placeholder="Minimal 6 karakter"/></label><button className="btn full">{isReg?'Daftar Sekarang':'Login'}</button></form><span className="switch">{isReg?'Sudah punya akun?':'Belum punya akun?'} <Link to={isReg?'/login':'/register'}>{isReg?'Login':'Register'}</Link></span></div></div>}
