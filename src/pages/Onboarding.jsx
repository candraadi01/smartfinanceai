import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { saveUser } from '../services/storage';
export default function Onboarding(){const nav=useNavigate();const [form,setForm]=useState({name:'Candra Adi',email:'candra@student.com',monthlyIncome:4300000,savingTarget:1200000,avatar:'CA'});function submit(e){e.preventDefault();saveUser(form);nav('/dashboard')}return <div className="auth-page"><div className="auth-card wide"><div className="eyebrow">Personal setup</div><h1>Set target keuanganmu</h1><p>Data ini dipakai untuk membuat dashboard dan rekomendasi lebih personal.</p><form onSubmit={submit} className="grid-form"><label>Nama<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Pemasukan Bulanan<input type="number" value={form.monthlyIncome} onChange={e=>setForm({...form,monthlyIncome:Number(e.target.value)})}/></label><label>Target Tabungan<input type="number" value={form.savingTarget} onChange={e=>setForm({...form,savingTarget:Number(e.target.value)})}/></label><button className="btn full span-2">Masuk Dashboard</button></form></div></div>}
