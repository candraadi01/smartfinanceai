import { useState } from 'react';
import { formatIDR, getUser, saveUser } from '../services/storage';
export default function Profile(){const [user,setUser]=useState(getUser());function submit(e){e.preventDefault();saveUser(user);alert('Profile tersimpan')}return <section className="page"><div className="page-title"><div><p>Account settings</p><h1>Profile</h1></div></div><div className="profile-grid"><div className="panel profile-card"><div className="big-avatar">{user.avatar}</div><h2>{user.name}</h2><p>{user.email}</p><div className="profile-metrics"><span>Income <b>{formatIDR(user.monthlyIncome)}</b></span><span>Target <b>{formatIDR(user.savingTarget)}</b></span></div></div><form className="panel tx-form" onSubmit={submit}><h2>Edit Profile</h2><label>Nama<input value={user.name} onChange={e=>setUser({...user,name:e.target.value})}/></label><label>Email<input value={user.email} onChange={e=>setUser({...user,email:e.target.value})}/></label><label>Pemasukan Bulanan<input type="number" value={user.monthlyIncome} onChange={e=>setUser({...user,monthlyIncome:Number(e.target.value)})}/></label><label>Target Tabungan<input type="number" value={user.savingTarget} onChange={e=>setUser({...user,savingTarget:Number(e.target.value)})}/></label><button className="btn full">Simpan Perubahan</button></form></div></section>}
