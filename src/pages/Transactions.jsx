import { useState } from 'react';
import { Plus, Trash2 } from 'lucide-react';
import { categories } from '../data/mock';
import { addTransaction, deleteTransaction, formatIDR, getTransactions } from '../services/storage';
export default function Transactions(){const [items,setItems]=useState(getTransactions());const [form,setForm]=useState({title:'',type:'expense',category:'Food',amount:'',date:new Date().toISOString().slice(0,10),note:''});function submit(e){e.preventDefault();if(!form.title||!form.amount)return;setItems(addTransaction({...form,amount:Number(form.amount)}));setForm({...form,title:'',amount:'',note:''})}return <section className="page"><div className="page-title"><div><p>Kelola cashflow</p><h1>Transactions</h1></div></div><div className="tx-layout"><form className="panel tx-form" onSubmit={submit}><h2>Tambah Transaksi</h2><label>Judul<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Contoh: makan siang"/></label><div className="two"><label>Tipe<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option value="expense">Expense</option><option value="income">Income</option></select></label><label>Kategori<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><label>Nominal<input type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} placeholder="100000"/></label><label>Tanggal<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>Catatan<input value={form.note} onChange={e=>setForm({...form,note:e.target.value})} placeholder="Opsional"/></label><button className="btn full"><Plus size={18}/> Simpan</button></form><div className="panel tx-list"><div className="panel-head"><h2>Riwayat Transaksi</h2><span>{items.length} data</span></div>{items.map(item=><div className="tx-item" key={item.id}><div className={`tx-dot ${item.type}`}></div><div><b>{item.title}</b><p>{item.category} • {item.date}</p></div><strong className={item.type}>{item.type==='income'?'+':'-'}{formatIDR(item.amount)}</strong><button onClick={()=>setItems(deleteTransaction(item.id))} className="delete"><Trash2 size={17}/></button></div>)}</div></div></section>}
